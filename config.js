const CONFIG = {
  GITHUB_OWNER: 'vicentedomus',
  GITHUB_REPO: 'bitcho',
  BITSO_API: 'https://api.bitso.com/v3',
  INITIAL_BALANCE_MXN: 10000,
  DATA_BRANCH: 'main',
  // GitHub PAT se guarda en localStorage, nunca en código
  // API key de Anthropic se guarda en n8n credentials, nunca en código
};
